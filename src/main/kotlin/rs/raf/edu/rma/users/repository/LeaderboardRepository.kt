package rs.raf.edu.rma.users.repository

import org.jetbrains.exposed.sql.Database

// STUB — replaced by Phase 5 (Task 14). Constructor signature must match UsersModule.kt wiring.
class LeaderboardRepository(database: Database)
